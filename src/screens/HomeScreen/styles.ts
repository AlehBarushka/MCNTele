import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 8,
  },
  filterContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
});

export default styles;
