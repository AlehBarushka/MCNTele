import { StyleSheet } from "react-native";
import { Colors } from "../../constants/colors";

const styles = StyleSheet.create({
  btnContainer: {
    padding: 16,
    backgroundColor: Colors.btn,
    borderRadius: 16,
  },
  btnContent: { fontSize: 18, color: Colors.white },
});

export default styles;
